import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    videoDownloadForm: {
        marginTop: 10,
        justifyContent: "center",
        alignItems: "center"
    },
    selectVideoFormatButtonStyle: {
        width: 300,
        borderRadius: 5,
        backgroundColor: "#fff",
        borderColor: "#ccc",
        borderWidth: 1
    },
    selectVideoFormatButtonText: {
        fontSize: 12
    },
    selectVideoFormatRowStyle: {
        width: 260
    },
    downloadButton: {
        width: 300,
        marginTop: 10,
        justifyContent: "center",
        alignItems: "center"
    }
})

export default styles