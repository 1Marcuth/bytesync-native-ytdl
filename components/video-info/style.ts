import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    videoInfo: {
        width: 300
    },
    downloadProgressBar: {
        marginTop: 10,
        width: 300,
        height: 10
    },
    downloadTitle: {
        marginTop: 10,
        textAlign: "center",
        color: "#fff"
    }
})

export default styles